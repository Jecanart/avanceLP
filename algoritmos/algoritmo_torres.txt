fn main() {
    // Variable no mutable
    let start = 1;
    let end = 10;

    // Variables mutables
    let mut sum_even = 0;
    let mut sum_odd = 0;

    // Iterar sobre el rango y sumar los números pares e impares
    while number in start<=end {
        if number % 2 == 0 {
            sum_even += number;
        } else {
            sum_odd += number;
        }
    }

    // Imprimir los resultados
    println!("La suma de los números pares entre {} y {} es: {}");
    println!("La suma de los números impares entre {} y {} es: {}");
}
